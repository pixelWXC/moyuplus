# VS Code TXT 阅读器与打字练习插件需求文档

## 1. 产品概述

本插件是一个运行在 VS Code 内部的本地工具，核心能力包括：

1. TXT 文件阅读器
2. 基于 TXT 文件的打字练习
3. 阅读器与打字练习相关快捷键映射
4. 阅读状态与练习状态的本地持久化

插件不依赖联网能力。初期主要用于个人体验与验证，若后续体验稳定、功能成熟，可考虑开源。

本插件不是完整电子书软件，也不是严格意义上的打字测速软件。它更接近一个在 VS Code 内使用的轻量阅读与自由打字练习工具。

核心定位：

> 在 VS Code 中提供一个侧边栏 TXT 阅读器，并允许用户将导入过的 TXT 文件作为打字练习素材，在任意编辑器文件中以行内提示的方式进行自由打字练习。

---

## 2. 使用场景

### 2.1 TXT 阅读

用户可以在 VS Code 内导入 TXT 文件，并在侧边栏阅读器中分页阅读。

阅读器需要支持：

* TXT 文件导入
* 已导入文件列表
* 分页阅读
* 阅读进度记忆
* 上一页 / 下一页
* 打开 / 隐藏阅读器
* 关闭阅读器
* 切换阅读文件
* 字体大小调整
* 常用阅读操作的快捷键映射

阅读器主要用于轻量阅读场景，尤其是用户在 VS Code 工作区中同时进行开发、阅读、练习等操作时。

---

### 2.2 打字练习

用户可以在阅读器设置或插件设置入口中开启打字练习功能。

开启打字练习时，用户需要选择一个已经导入过的 TXT 文件作为练习素材。

练习文件以“物理行”为单位，每一行是一条练习内容。

在打字练习开启后，用户可以在当前 VS Code 工作区的任意文件中进行打字练习。插件会在当前编辑器光标所在行显示当前练习行的行内半透明提示文本。

用户可以参考提示文本进行输入，但不强制完全一致。

---

## 3. 基本产品原则

### 3.1 本地优先

插件不联网，不上传文件，不依赖远程服务。

TXT 文件读取、阅读进度、练习进度、快捷键配置等信息都保存在本地。

---

### 3.2 阅读与练习互相独立

阅读器和打字练习是两个独立功能。

用户可以：

* 阅读 `a.txt`
* 同时使用 `b.txt` 进行打字练习

阅读进度不会影响打字练习进度。

打字练习进度也不会影响阅读进度。

即使阅读文件和练习文件是同一个 TXT 文件，两套状态也不自动同步。

---

### 3.3 不干扰 VS Code 原生编辑体验

插件需要尽量避免破坏 VS Code 原有的编辑行为。

尤其是：

* Enter
* Tab
* 文本补全
* snippet 跳转
* 编辑器原生命令
* 其他插件的补全行为

快捷键功能需要通过明确的上下文、状态和优先级控制，避免误触或冲突。

---

## 4. TXT 文件管理

### 4.1 文件导入方式

导入 TXT 文件时，插件只保存原文件路径，不复制文件内容到插件内部。

也就是说，插件维护的是一个 TXT 文件索引，而不是一个独立书库副本。

文件索引中需要保存：

* 文件 ID
* 文件名
* 文件路径
* 编码类型
* 文件来源
* 阅读进度
* 打字练习进度
* 最近使用时间

---

### 4.2 文件来源

TXT 文件可以来自：

* 当前 VS Code 工作区内
* 当前 VS Code 工作区外

插件需要同时支持这两类来源。

如果文件来自工作区外，需要在文件列表中能够识别其来源。

---

### 4.3 文件失效处理

由于插件只保存文件路径，如果原始 TXT 文件被移动、删除或重命名，插件再次访问该文件时可能失败。

此时插件需要提示用户：

* 文件不存在
* 文件路径失效
* 是否重新定位该文件
* 是否从导入列表中移除该文件

---

### 4.4 编码支持

插件需要支持以下 TXT 编码：

* UTF-8
* GBK

导入或读取 TXT 文件时，需要允许用户选择或切换编码。

---

### 4.5 文件列表

插件只需要提供简单的文件列表，不需要复杂书架、分类、标签系统。

文件列表至少需要支持：

* 查看已导入 TXT
* 切换阅读文件
* 选择打字练习文件
* 移除已导入记录
* 识别当前阅读文件
* 识别当前练习文件

---

## 5. 阅读器功能

### 5.1 阅读器位置

阅读器采用 VS Code 侧边栏阅读器形态。

阅读器应作为 VS Code 插件提供的侧边栏视图存在，用户可以在 VS Code 内打开、隐藏或关闭该阅读器。

---

### 5.2 阅读模式

阅读器采用分页模式。

不采用连续滚动阅读作为核心模式。

用户通过上一页、下一页等操作进行翻页阅读。

---

### 5.3 动态分页

分页不按固定行数计算，也不按固定字符数计算。

阅读器需要根据当前阅读窗口的实际显示区域进行动态分页。

分页计算需要考虑：

* 阅读器容器宽度
* 阅读器容器高度
* 字体大小
* 字体类型
* 行高
* 内边距
* 文本换行
* 中文、英文、标点、空格等实际渲染宽度
* 单行过长时自动换行后的高度
* 预格式文本、表格和连续长字符的横向边界

目标是：

> 在当前阅读器窗口中尽可能填满可见区域，同时避免任何横向或纵向文本溢出。

---

### 5.4 单行过长处理

当 TXT 或 EPUB 中出现长连续文本、预格式文本或宽表格时，不应横向滚动或隐藏裁切。

应根据阅读器宽度自动换行。

自动换行后的内容需要计入当前页高度计算。

EPUB 正文仅保留标题、段落、列表、引用、表格、代码、脚注、尾注和内部目标等安全语义结构。出版物 CSS、内联样式、表现 class、字体、颜色、尺寸、边距、多栏、浮动和固定定位不进入 Reader；页面四边距只由 MoyuPlus 阅读设置控制。

---

### 5.5 分页测量方式

动态分页采用 Webview DOM 精准测量方式。

也就是说，分页结果应以真实 DOM 渲染后的高度为依据，而不是通过简单字符数估算。

可接受的实现思路是：

* 从当前字符 offset 开始取一段文本
* 渲染到隐藏测量容器
* 测量实际宽度和高度
* 判断横向或纵向是否超过阅读器可见区域
* 通过查找算法确定当前页最多可容纳的文本范围

---

### 5.6 阅读进度

阅读进度按字符级 offset 记录。

阅读器关闭后，再次打开时应恢复到上次阅读的大致位置。

如果用户没有调整阅读器窗口大小、字体大小、行高等影响分页的参数，那么恢复后的显示位置应尽可能与上次一致。

如果显示区域或字体参数发生变化，则不要求页码完全一致，但需要保证恢复到接近上次阅读位置的内容附近。

---

### 5.7 翻页行为

阅读器支持：

* 下一页
* 上一页
* 撤回阅读位置

翻页时默认不保留上一页的重叠文本。

即：

* 下一页从当前页结束位置之后继续
* 上一页回到上一页起始位置附近

阅读器需要维护翻页历史或可逆分页状态，以保证上一页操作能够尽可能回到用户刚才看到的上一屏内容。

---

### 5.8 阅读器关闭与恢复

关闭阅读器后，需要保留：

* 当前阅读文件
* 当前阅读 offset
* 当前阅读相关设置

再次打开阅读器时，应恢复上次阅读状态。

---

## 6. 打字练习功能

### 6.1 练习文件选择

开启打字练习时，用户必须选择一个已经导入过的 TXT 文件。

该文件作为当前练习素材。

练习文件和阅读器当前阅读文件相互独立。

---

### 6.2 练习单位

打字练习以 TXT 文件的物理行为单位。

每一物理行对应一条练习内容。

插件不自动按句号、标点、字符长度或视觉宽度拆分练习内容。

---

### 6.3 空行与空格处理

练习模式需要提供以下配置：

* 跳过空行
* 忽略前导空格
* 忽略所有空格

这些配置只影响打字练习的提示和行切换逻辑，不影响原始 TXT 文件内容。

---

### 6.4 练习开始位置

打字练习默认从上次练习进度继续。

同时需要支持：

* 重置到第一行
* 跳转到指定行

---

### 6.5 行内提示

打字练习开启后，插件需要在当前 VS Code 编辑器的光标所在行显示当前练习行内容。

提示形式为行内半透明文本，即类似 ghost text 的效果。

用户可以参考该提示进行输入。

---

### 6.6 非严格输入

用户不需要完全按照 TXT 内容输入。

当用户输入内容和当前练习行不一致时，插件不提示错误。

不需要实时标红。

不需要阻止用户继续输入。

不需要强制纠正。

打字练习的核心目标是提供参考文本，而不是进行严格判题。

---

### 6.7 Tab 补全行为

打字练习开启时，Tab 可以用于补全当前练习行。

Tab 补全支持两种可配置模式：

#### 模式 A：替换整行

用当前 TXT 练习行内容替换编辑器当前行内容。

适合用户希望快速跳过当前内容，或将当前行修正为目标文本的场景。

#### 模式 B：只补全剩余部分

根据用户已经输入的内容，只补全当前练习行尚未输入的剩余内容。

适合更自然的打字练习体验。

---

### 6.8 Enter 行为

打字练习开启时，按 Enter 必须保留 VS Code 原本的真实换行行为。

也就是说，Enter 至少应该执行：

* 在当前编辑器中插入真实换行

除此之外，Enter 是否同时触发以下行为，需要作为可配置项：

* 打字练习切换到下一行
* 阅读器翻到下一页

因此 Enter 是一个组合行为，而不是单一行为。

---

### 6.9 编辑器切换

打字练习开启后，用户切换到当前工作区的其他文件时，练习状态继续存在。

当前练习文件、练习行号、练习配置不因为编辑器切换而重置。

用户可以在任意文件中继续当前打字练习。

---

### 6.10 安全提示

由于打字练习允许在任意文件中进行，插件需要提示用户：

* 练习输入会真实写入当前编辑器文件
* 建议在临时文件、草稿文件或专门练习文件中使用
* 避免误修改项目源码

---

## 7. 阅读器与打字练习的关系

阅读器和打字练习状态完全独立。

### 7.1 文件独立

允许：

* 阅读器正在阅读 `a.txt`
* 打字练习正在使用 `b.txt`

两者互不影响。

---

### 7.2 进度独立

阅读器翻页不会影响打字练习行号。

打字练习切换下一行不会影响阅读器阅读 offset。

---

### 7.3 即使文件相同也不同步

如果用户同时阅读和练习同一个 TXT 文件，也不自动同步阅读进度和练习进度。

---

## 8. 快捷键映射

### 8.1 快捷键配置入口

插件需要提供插件内快捷键设置页面。

用户可以在插件设置页面中配置主要功能对应的快捷键。

同时，底层实现需要尊重 VS Code 原生快捷键机制与上下文状态，避免破坏原有编辑器行为。

---

### 8.2 需要支持快捷键的阅读器功能

以下阅读器功能需要支持快捷键映射：

* 下一页
* 上一页
* 关闭阅读器
* 打开 / 隐藏阅读器
* 切换阅读文件
* 增大字体
* 减小字体
* 开启 / 关闭打字练习

---

### 8.3 Enter 组合行为

Enter 行为需要可配置。

Enter 可能同时承担：

* 编辑器正常换行
* 打字练习切换到下一行
* 阅读器翻到下一页

默认必须保证编辑器正常换行能力不被破坏。

如果用户启用了 Enter 翻页，则 Enter 可以在正常换行的同时触发阅读器下一页。

如果用户启用了 Enter 切换练习行，则 Enter 可以在正常换行的同时让练习进度前进一行。

---

### 8.4 Tab 优先级

Tab 行为需要兼容 VS Code 原生机制。

优先级应为：

1. 如果 VS Code 补全菜单正在显示，Tab 优先执行原生补全行为
2. 如果 snippet 模式正在激活，Tab 优先跳转 snippet 占位符
3. 如果打字练习开启，Tab 执行练习行补全
4. 否则执行 VS Code 原生 Tab 行为

---

### 8.5 快捷键冲突处理

插件不能承诺所有快捷键都绝对不冲突。

但插件需要做到：

* 默认快捷键尽量避开 VS Code 高频快捷键
* 高风险按键需要有明确上下文限制
* 用户自定义快捷键时应尽量提示潜在冲突
* Enter、Tab 等特殊键必须通过组合行为和优先级判断处理
* 不应在打字练习未开启时拦截正常编辑行为
* 上一页、下一页和撤回阅读位置默认不绑定按键，由用户在 VS Code Keyboard Shortcuts 中按需设置
* 书架、Git Log 或无活动正文时，阅读位置命令应静默无操作

---

## 9. 设置面板

插件设置入口采用组合方式：

* 阅读器 Webview 内提供常用设置
* VS Code Settings 中提供高级配置
* Command Palette 中提供快捷命令
* 插件内提供快捷键设置页面

---

### 9.1 阅读器内设置

阅读器侧边栏中应提供与阅读直接相关的设置，例如：

* 切换 TXT 文件
* 字体大小
* 编码切换
* 上一页
* 下一页
* 撤回阅读位置
* EPUB 内部目录、脚注、尾注和返回链接导航
* EPUB 内部图片的只读预览入口
* 打开打字练习设置
* 关闭阅读器

---

### 9.2 打字练习设置

打字练习设置中应提供：

* 开启 / 关闭打字练习
* 选择练习 TXT 文件
* 重置练习进度
* 跳转到指定行
* 跳过空行
* 忽略前导空格
* 忽略所有空格
* Tab 补全模式
* Enter 组合行为

---

### 9.3 快捷键设置

快捷键设置页面中应至少显示：

* 功能名称
* 当前绑定按键
* 是否启用
* 是否存在潜在冲突
* 对应动作说明

---

## 10. 状态栏

打字练习开启时，VS Code 状态栏需要显示当前练习状态。

状态栏显示内容应包含：

* 当前练习 TXT 文件名
* 当前练习行号
* 总行数

示例：

```txt
Typing: b.txt 12/300
```

---

### 10.1 状态栏点击行为

点击状态栏后，应展示打字练习快捷菜单。

菜单可包含：

* 暂停 / 继续练习
* 切换练习文件
* 跳到指定行
* 重置到第一行
* 关闭打字练习

---

## 11. 数据存储策略

### 11.1 文件列表

已导入 TXT 文件列表全局共享。

也就是说，用户在不同 VS Code 工作区中都可以看到同一批已导入 TXT 文件。

---

### 11.2 阅读与练习状态

阅读状态和打字练习状态按 workspace 独立。

这意味着：

* TXT 文件列表是全局的
* 每个工作区可以有自己的当前阅读文件
* 每个工作区可以有自己的阅读 offset
* 每个工作区可以有自己的练习文件
* 每个工作区可以有自己的练习行号
* 每个工作区可以有自己的练习配置

---

## 12. 建议的数据模型

### 12.1 已导入 TXT 文件

```ts
type ImportedTxtFile = {
  id: string;
  name: string;
  uri: string;
  encoding: 'utf8' | 'gbk';
  source: 'workspace' | 'external';
  createdAt: number;
  updatedAt: number;
  lastOpenedAt?: number;
};
```

---

### 12.2 阅读状态

```ts
type ReaderSession = {
  active: boolean;
  fileId?: string;
  offset: number;
  approximatePercent: number;
  fontSize: number;
  lineHeight: number;
  viewportSnapshot?: {
    width: number;
    height: number;
    fontSize: number;
    lineHeight: number;
  };
  pageHistory: Array<{
    startOffset: number;
    endOffset: number;
  }>;
};
```

---

### 12.3 打字练习状态

```ts
type TypingPracticeSession = {
  active: boolean;
  fileId?: string;
  lineIndex: number;
  totalLines: number;
  skipEmptyLines: boolean;
  trimLeadingSpaces: boolean;
  ignoreAllSpaces: boolean;
  tabMode: 'replaceLine' | 'completeRest';
  enterBehavior: {
    insertNewLine: boolean;
    nextPracticeLine: boolean;
    nextReaderPage: boolean;
  };
};
```

---

### 12.4 快捷键配置

```ts
type ShortcutConfig = {
  nextPage?: string;
  previousPage?: string;
  closeReader?: string;
  toggleReader?: string;
  switchReaderFile?: string;
  increaseFontSize?: string;
  decreaseFontSize?: string;
  toggleTypingPractice?: string;
  enterRouter?: string;
  tabRouter?: string;
};
```

---

## 13. 关键技术理解

### 13.1 阅读器 UI

阅读器适合使用 VS Code Webview View 实现。

Webview 负责展示侧边栏阅读器 UI、阅读内容、设置面板和用户操作入口。

---

### 13.2 动态分页

动态分页应在 Webview 中基于真实 DOM 测量完成。

扩展主进程负责：

* 读取文件
* 保存状态
* 管理会话
* 响应命令

Webview 负责：

* 渲染阅读内容
* 测量正文宽度和高度
* 计算当前页可容纳文本范围
* 通知扩展当前页起止 offset

---

### 13.3 打字练习提示

打字练习中的半透明文本提示适合使用 VS Code 的行内补全能力实现。

插件在练习开启时，为当前编辑器光标位置提供当前练习行内容作为 ghost text。

练习关闭时，不应继续提供任何行内提示。

---

### 13.4 命令路由

Enter、Tab 等特殊按键不应直接绑定到单一动作，而应绑定到路由命令。

路由命令根据当前上下文判断实际行为。

例如 Enter 路由需要判断：

* 是否需要正常换行
* 打字练习是否开启
* 是否启用 Enter 切换下一练习行
* 阅读器是否开启
* 是否启用 Enter 翻到下一页

Tab 路由需要判断：

* 是否存在原生补全菜单
* 是否处于 snippet 模式
* 是否处于打字练习状态
* 当前 Tab 补全模式是什么

---

## 14. 非目标功能

以下内容不属于当前已确认需求范围：

* 联网同步
* 云端文件存储
* 用户账号系统
* 严格打字判题
* 实时错误标红
* WPM 统计
* 正确率统计
* 练习历史记录
* 复杂书架系统
* 标签分类系统
* 阅读进度与练习进度自动同步
* 多阅读会话同时打开
* 同时打开多个阅读窗口并分别维护状态

---

## 15. 已确认的关键决策汇总

| 模块       | 决策                                           |
| -------- | -------------------------------------------- |
| 插件定位     | 本地自用优先，不联网，好用后考虑开源                           |
| 阅读器位置    | VS Code 侧边栏阅读器                               |
| 阅读模式     | 分页模式                                         |
| 分页方式     | 根据窗口真实尺寸动态分页                                 |
| 分页测量     | Webview DOM 精准测量                             |
| 阅读进度     | 字符级 offset                                   |
| 翻页重叠     | 默认无重叠                                        |
| 阅读会话     | 只允许一个阅读会话                                    |
| TXT 导入   | 保存原文件路径，不复制文件                                |
| 文件来源     | 工作区内外 TXT 都支持                                |
| 编码       | UTF-8 + GBK                                  |
| 文件管理     | 简单文件列表                                       |
| 阅读与练习    | 完全独立                                         |
| 练习单位     | 每一物理行是一条练习内容                                 |
| 练习进度     | 默认继续，可重置，可跳指定行                               |
| 输入校验     | 不校验，不提示错误                                    |
| Tab 行为   | 可配置整行替换或补全剩余部分                               |
| Enter 行为 | 必须真实换行，其他行为可组合配置                             |
| 编辑器切换    | 练习继续                                         |
| 状态栏      | 显示练习文件名、当前行号、总行数                             |
| 状态栏点击    | 展示练习快捷菜单                                     |
| 快捷键      | 插件内提供设置页面                                    |
| Tab 优先级  | 兼容补全菜单、snippet、练习补全、原生 Tab                   |
| 设置入口     | Webview + VS Code Settings + Command Palette |
| 数据范围     | 文件列表全局，阅读/练习状态按 workspace 独立                 |
